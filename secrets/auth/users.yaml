# User authentication secrets
#
# Generate a hashed password:
#   mkpasswd -m sha-512
#
# Encrypt: sops --encrypt --in-place secrets/auth/users.yaml

users:
    elyria:
        # Replace with output from: mkpasswd -m sha-512
        password: "$6$rounds=656000$PLACEHOLDER$PLACEHOLDER"
