# Backblaze B2 credentials for rclone
#
# Encrypt: sops --encrypt --in-place secrets/api/backblaze.yaml
#
# To get these values:
#   1. Log into Backblaze B2 → App Keys
#   2. Create a new application key (limit to one bucket)
#   3. Copy the keyID and applicationKey

backblaze:
    key_id: "PLACEHOLDER"
    application_key: "PLACEHOLDER"
