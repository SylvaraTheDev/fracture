# SSH secrets
#
# For SSH private keys, store the key content as a multiline string.
# sops-nix will write it to a file accessible only by the owning user.
#
# Encrypt: sops --encrypt --in-place secrets/ssh/config.yaml

ssh:
    # host_key: |
    #     -----BEGIN OPENSSH PRIVATE KEY-----
    #     ...
    #     -----END OPENSSH PRIVATE KEY-----
    # elyria_ed25519: |
    #     -----BEGIN OPENSSH PRIVATE KEY-----
    #     ...
    #     -----END OPENSSH PRIVATE KEY-----
